# RPG-
Good
<!DOCTYPE html> <html lang="ko"> <head> <meta charset="UTF-8"> <title>Infinite Online RPG</title> <meta name="viewport" content="width=device-width, initial-scale=1"> <style> body{   margin:0; background:#0b0e14; color:#e6e6e6;   font-family: "Segoe UI", sans-serif; } #app{max-width:900px;margin:auto;padding:10px;} .panel{   background:#141824;border-radius:8px;   padding:10px;margin-bottom:10px;   box-shadow:0 0 10px rgba(0,0,0,.5); } h2,h3{margin:5px 0;color:#6cf;} #gameLog,#chatLog{   height:180px; overflow-y:auto;   background:#0b0e14; padding:8px;   border-radius:6px; font-family:monospace; } input,button{   width:100%; padding:8px;   background:#1b2133;color:#fff;   border:none;border-radius:6px;   margin-top:5px; } button:hover{background:#263055;} .flex{display:flex;gap:5px;} .small{font-size:12px;color:#aaa;} </style> </head> <body>  <div id="app">  <div class="panel">   <h2>🌍 Infinite Online RPG</h2>   <div class="small" id="statusBar"></div> </div>  <div class="panel">   <h3>🎮 게임</h3>   <div id="gameLog"></div>   <div class="flex">     <input id="cmd" placeholder="move / explore / rest / status / nick">     <button onclick="gameCmd()">실행</button>   </div> </div>  <div class="panel">   <h3>💬 실시간 채팅</h3>   <div id="chatLog"></div>   <div class="flex">     <input id="chatInput" placeholder="채팅 입력">     <button onclick="sendChat()">보내기</button>   </div> </div>  </div>  <!-- Firebase --> <script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-app.js"></script> <script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-database.js"></script>  <script> /* 🔥 Firebase 설정 교체 */ const firebaseConfig = {   apiKey: "YOUR_API_KEY",   authDomain: "YOUR_PROJECT.firebaseapp.com",   databaseURL: "https://YOUR_PROJECT.firebaseio.com",   projectId: "YOUR_PROJECT",   storageBucket: "YOUR_PROJECT.appspot.com",   messagingSenderId: "XXXX",   appId: "XXXX" }; firebase.initializeApp(firebaseConfig); const db = firebase.database();  /* ===== 유저 ===== */ let player = JSON.parse(localStorage.getItem("player")) || {   nick: prompt("닉네임 입력"),   x:0, y:0, hp:100, sanity:100 }; localStorage.setItem("player", JSON.stringify(player));  /* ===== UI ===== */ const gameLog = document.getElementById("gameLog"); const chatLog = document.getElementById("chatLog"); const statusBar = document.getElementById("statusBar");  function logGame(t){   gameLog.innerText += t + "\n";   gameLog.scrollTop = gameLog.scrollHeight; } function updateStatus(){   statusBar.innerText =   `닉네임: ${player.nick} | HP:${player.hp} | 정신:${player.sanity} | 위치 (${player.x},${player.y})`; }  /* ===== 게임 ===== */ function terrain(x,y){   const list=["숲","사막","폐허","설원","이상한 평원"];   return list[Math.abs((x*31+y*17)%list.length)]; }  function gameCmd(){   const c = cmd.value.toLowerCase().trim();   cmd.value="";    if(c==="move"){     player.x+=Math.random()<0.5?1:-1;     player.y+=Math.random()<0.5?1:-1;     logGame(`이동 → ${terrain(player.x,player.y)}`);   }   else if(c==="explore"){     if(Math.random()<0.6){       player.hp-=10; player.sanity-=5;       logGame("⚔️ 적과 조우!");     }else logGame("조용하다…");   }   else if(c==="rest"){     player.hp=Math.min(100,player.hp+20);     player.sanity=Math.min(100,player.sanity+5);     logGame("휴식했다");   }   else if(c==="status"){     logGame(`HP:${player.hp} 정신:${player.sanity}`);   }   else if(c==="nick"){     player.nick=prompt("새 닉네임");     postSystem(`${player.nick} 닉네임 변경`);   }   else logGame("알 수 없는 명령");    if(player.x===0 && player.y===0 && player.sanity<50){     logGame("🐣 이스터에그: 세계가 너를 보고 있다");     player.sanity+=10;   }    localStorage.setItem("player", JSON.stringify(player));   updateStatus(); }  /* ===== 실시간 채팅 ===== */ function sendChat(){   const t = chatInput.value;   if(!t) return;   chatInput.value="";   db.ref("chat").push({     nick: player.nick,     text: t,     time: Date.now()   }); }  function postSystem(text){   db.ref("chat").push({     nick: "SYSTEM",     text: text,     time: Date.now()   }); }  db.ref("chat").limitToLast(100).on("child_added", snap=>{   const m=snap.val();   chatLog.innerText += `[${m.nick}] ${m.text}\n`;   chatLog.scrollTop = chatLog.scrollHeight; });  updateStatus(); 
